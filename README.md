# divoVAM

CindyJS widgets for mathematics education — interactive manipulatives ("virtuelle
Arbeitsmittel", VAM) built in [Cinderella](https://cinderella.de) and deployed
two ways: as a component inside the [divomath](https://divomath-nrw.de) platform,
and as a standalone HTML page.

Current build: **v7b427**


## The two deliverables

One source, two targets. `build.py` produces both from the same Cinderella
construction:

| Output | Goes to | What it is |
|---|---|---|
| `out/divoVAM.html` | `abako.dzlm.de/cindy/vam/` | Self-contained page, configured through URL parameters |
| `out/divoVAM.cdyjs` | divomath import | Module form, configured through the divomath editor |
| `out/freehand-drawing.js` | next to the HTML | Pen overlay, only loaded with `?draw` |

Which widget is shown is a parameter, not a separate file — one build carries all
of them.

## Widgets

- `distributive`: Rectangular array model
- `divisors`: Division and remainders
- `numbercards`: Place value understanding
- `percentagebar`: Just an interactive percentagebar
- `strapwork`: Recognizing patterns
- `thales`: Thales' theorem and it's inverse

Two more are under construction but unfinished and should not be used in production:
- `doublenumberline`
- `localderivate`.

---

## Requirements

- **Cinderella 3.0**, build 2131 or later — to open and edit the construction
- **Python 3** — no third-party packages, standard library only
- **CindyJS 0.8** — not vendored; the exported HTML loads it from `cindyjs.org/dist/v0.8/`

## Repository layout

```bash
├─ divoVAM.cdy # Cinderella construction — the actual source
├─ divoVAM.html # Cinderella's HTML export
├─ build.py # Build script
├─ out/ # gets generated by build.py (NOT IN VERSION CONTROL)
    ├─ divoVAM.html
    ├─ divoVAM.cdyjs
    └─ freehand-drawing.js
├─ docs/
    └─ doc-german.md # User documentation (german)
├─ js/
    └─ freehand-drawing.js # the pen overlay 
├─ assets/
    └─ img/
        └─ ... # Icons used by the widgets
├─ special embeds/ # Ready-made embeddings
    └─ storyline/ # Storyline embedding
├─ CHANGELOG.md # Version history
└─ README.md # this file
```


## Build

```bash 
python3 build.py "<cdy-filename>"
```

The full loop:

1. Edit in Cinderella
2. **Save** the `.cdy`
3. **Export** the HTML over the existing `divoVAM.html`
4. `python3 build.py "divoVAM"`
5. Upload the contents of `out/`

Steps 2 and 3 are both needed, and the order matters. The build warns when the
two files disagree by more than a minute — that warning almost always means the
save or the export was skipped, and building anyway would mix two states 
without anything visible in the result.

**Why two sources?** Cinderella's HTML export silently omits some scripts. The
scripts are therefore read out of the `.cdy` archive, which always holds the
complete set. Everything else — geometry, appearance, animation — is taken from
the export, so no template needs hand-maintaining.

### Options

| Flag | Effect |
|---|---|
| `--out DIR` | Output directory (default `out`) |
| `--img-base URL` | Base URL the `.cdyjs` loads icons from |
| `--rect A,B,C,D` | `visibleRect` written into the `.cdyjs` |
| `--html-only` / `--cdyjs-only` | Build just one target |


## URL parameters (standalone build)

Widget parameters are documented in [docs/doc-german.md](docs/doc-german.md).
The ones below belong to the page itself:

| Parameter | Effect |
|---|---|
| `?vam=<name>` | Which widget to show |
| `?full` | Canvas fills the browser window |
| `?rect=divomath` | 24.5 × 18.9 world units (default) |
| `?rect=classic` | 38.2 × 22.1 |
| `?rect=wide` | The viewport of the former `divoVAM-full.html` |
| `?rect=a,b,c,d` | Custom rect `[left, top, right, bottom]` |
| `?draw` | Freehand pen overlay, plus `drawpen`, `drawerase`, `drawpos`, `drawdir`, `drawmoveable` |

There is a form-based link generator for these at
<https://abako.dzlm.de/cindy/vam/>. That page has to be updated by hand 
when new parameters are added.



## Working on the framework

Everything lives in the construction's **Init** script, split into sections that
Cinderella shows as separate sub-scripts. The prefix tells you what a section is:

| Prefix | Contents |
|---|---|
| `[FUN]` | Free functions — drawing, geometry, strings, logging, divomath interface |
| `[FW]` | Constants, configuration, init entry and exit |
| `[C]` | Classes — `Button`, `Toggle`, `TextInput`, `Keyboard`, `ScrollBar`, `Workbench`, … |
| `[VAM]` | One section per widget. `[VAM*]` marks unfinished ones |

Order matters: sections run top to bottom, so a constant has to be defined above
its first use at init time.

### Conventions

- **Comments in English, user-facing documentation in German.** The audience 
for the docs is the German-speaking editorial team.
- **Global widget configuration** uses an apostrophe prefix: `'barwidth`,
`'showincolor`. Speaking names, no abbreviations.
- **Modifier parameters** use a `mod'` prefix — `mod'color`, `mod'font`. Without
  it an unset modifier captures a same-named variable from the surrounding scope.
- **Never name a variable** `color`, `size`, `alpha`, `font` or `bold`. Those
  shadow the drawing modifiers of the same name.
- **Scaling factors** (`FONTSCALE`, `UISCALE`, `IMGREFRESOLUTION`) are applied at
  exactly one point in the inheritance chain — where the size first comes into
  existence, and nowhere else.

### Debug keys

The running widget listens for a few keys, defined in the `[FW] keypressed`
script:

| Key | Effect |
|---|---|
| `y` | Dump the settings — `ISCINDYJS`, `screenbounds()`, `ENVIRONMENTALPARAMS`, `BGCOLOR`, `LOGLEVEL`, and the whole divomath block |
| `x` | Call `divomath set state()` and `divomath update results()` and print what they return |
| `c` | Print the stored log messages (`MAXLOGMESSAGES` of them) |
| `s` | Push results and state to divomath, as an interaction would |
| `+` / `-` | Raise or lower `LOGLEVEL` at runtime, clamped to 0..3 |

From level 2 upwards the widgets draw their own hitboxes; level 3 adds
`VISIBLERECT`, distance circles around the origin and the widget's docstring.
`?debuglevel=3` sets the same thing from the URL.

The build ships with `csconsole: false`, so there is no console element on the
page — but `println` still reaches the **browser's JavaScript console**, so the
keys are useful in the export and not just in Cinderella.

### CindyScript is not CindyJS

The construction runs in two engines that disagree in ways Cinderella hides.
Anything that gets drawn or serialised has to be checked in the export, because
plenty of things look right in the Cinderella window and are not:

| | Cinderella | CindyJS |
|---|---|---|
| `apply()` over a dictionary | returns the values | returns nothing — use `values()` |
| `if(___, a, b)` | — | runs *neither* branch, silently |
| `length(___)` | `0` | undefined |
| `pixelsize(..., font->)` | evaluates | "Modifier not supported" |
| `drawimage(..., ref->)` | anchors the image | ignored, image stays centred |

Function names are case- and space-insensitive (`list or` is `listor`); variable
names are case-sensitive.

---

## Documentation

- **[docs/doc-german.md](docs/doc-german.md)** — every widget parameter, the
  divomath state and validation model, the standalone URL parameters. In German,
  written for the people configuring the widgets rather than building them.
  Published at <https://abako.dzlm.de/cindy/vam/README.md>.
- **[CHANGELOG.md](CHANGELOG.md)** — version history

---

## Project context

Developed at Humboldt-Universität zu Berlin, Institut für Mathematik, for the
divomath NRW platform, in collaboration with TU Dortmund.
